import React from 'react'

type Props = {}

export default function SettingsAndCodeStep({ }: Props) {

    const onClickHandler = () => {


    }
    
    return (
        <div>
            <button onClick={onClickHandler}>generateCode</button>

        </div>
    )
}
